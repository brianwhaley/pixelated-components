/**
 * Google Search Console Integration Services
 * Server-side utilities for Google Search Console data retrieval
 */

"use server";

import { getSearchConsoleData, SearchConsoleConfig, SearchConsoleResponse } from './google.api.integration';