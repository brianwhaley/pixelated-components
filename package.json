{
  "name": "informationfocus",
  "version": "0.1.17",
  "private": true,
  "scripts": {
    "dev": "npm run clean && NODE_OPTIONS='--inspect' next dev  -p 3001 --experimental-https",
    "build": "npm run clean && NODE_ENV=production next build",
    "start": "npm run clean && next build && next start",
    "lint": "next lint",
    "clean": "rm -rf .next && rm -rf out"
  },
  "dependencies": {
    "@brianwhaley/pixelated-components": "^2.1.43",
    "next": "^15.5.6",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "sass": "^1.93.2"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@types/node": "^24.8.1",
    "@types/react": "^19",
    "@types/react-dom": "^19.2.2",
    "eslint": "^9",
    "eslint-config-next": "^15.5.6",
    "typescript": "^5"
  }
}
